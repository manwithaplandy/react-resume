# Final review preparation — provisional, not a review verdict

Prepared by the controller while E4 runs. Final review still needs the completed E4/E5/E6 diff and actual final acceptance outputs. This file does not close tasks or replace the review template.

## Scope and authoritative inputs

- Whole branch starts at 9b7f6fe; d6929f0 preserves the previously untracked audit and plan. Use the actual final merge-base and final HEAD when packaging, not HEAD~1 or a single task's base.
- Spec: reports/design-ux-review-2026-09-07.md, F01–F30 and original severities. Plan: docs/superpowers/plans/2026-09-07-design-ux-remediation.md and its subplans.
- Implementation root: this isolated worktree; original checkout's untracked docs/reports must remain preserved.
- WS/progress.md records completed task ranges, review fixes, deferred minors, exact rulings and costs. Every Ruling must appear in the final human handoff in chronological order.
- Later E6 may use e6-closure-preparation.md as an evidence index, but must reconcile it with final task reports and the actual acceptance pass.

## Cross-task boundaries worth reviewing

1. Complete graph information remains available through visible keyboard-accessible list/3D modes; route/query/history changes must preserve selection, search/reset state, full label wrapping and interruption/camera cleanup. Actual WebGL evidence and fallback evidence are distinct; do not silently equate unavailable WebGL with graph verification.
2. Contact validation, focus and single-attempt lifecycle must agree: trimmed snapshot, preserved raw draft, read-only pending fields, bounded timeout, uncertain delivery, actual SPA abort and no late completion feedback. All verification is intercepted; no real contact traffic is authorized.
3. Reader and producer must agree on v1 compatibility, null/zero/provisional observations, independent source units/coverage/success dates, aging and privacy thresholds. Accepted Cloudflare projection plus metadata must remain one checkpoint across failed/repeated/uncertain source writes.
4. Durable ingestion proofs, active guard, cursor and counter updates must prevent partial publication, duplicate application and skipped interrupted input, including disappearance/changed content, lost responses and exhausted budget. Preserve existing history and all internal records; no marker-first rollback or table reset.
5. Both bootstrap and ordinary producer installation must use the exact checked archive and require the actual compatible public reader. Terraform archive generation must not overwrite checked bytes. D3 extracted-runtime recovery tests must survive packaging centralization.
6. Checked 404 assets and document must precede error-policy activation. Routing preserves meaningful query values/multivalues, static paths and safe same-origin canonical redirects. VM/handcrafted events are not actual viewer encoding or deployed CloudFront verification; origin403 mapping also covers permission failures.
7. Cache metadata and policies must agree for stable60/300, hash1yearimmutable, and Lambda-owned stats60/300; cache minima must not undermine10second error handling. Actual uploader must validate manifest/file identities, upload hashes first, refresh known unchanged metadata, retain old hashes and never blanket-delete or own stats.
8. Reusable PR/main checks must be nonmutating and unprivileged until deployment; no cloud credentials/state/contact traffic in PR jobs. Build once and reuse checked frontend/archive artifacts; archive/provenance/reference validation and actual source-immutability checks should be meaningful. Workflow syntax/local command success is not a completed GitHub run or branch-protection enforcement.
9. Real final state-backed Terraform plan must classify every action, block unexpected replacement/data reset/identity/retention/secret-input drift, and preserve used provider pins. Self-logging relationship deletion is not bucket/data deletion. Private state/plan/input/raw-export data must not enter committed evidence.
10. The final report must retain original severity and exact open facts/account/manual/release dependencies. Preserve MIT attribution, historical review, reproducible PDF generation and current factual contexts. No unsupported owner claims, historical reconstruction, benchmark/savings, all-hops-encrypted, public freshness or deployed-verification claim.

## Final gate and record procedure

- Run the integrated acceptance pass once on final relevant source; broaden/repeat only for new failures/changes/concerns. Capture actual outputs as files as they run. Test counts for nested extracted-runtime reruns must not be double-counted as distinct cases.
- Perform the controller's consolidated real Terraform review in a private separate configuration environment with actual preserved inputs; publish a sanitized action summary only. Never apply as part of review.
- Independent whole-branch reviewer is /root/content_and_journeys (never implemented). Supply one packaged diff, actual report/evidence and the deferred/parked ledger entries; one fix wave and one scoped re-review if needed per SDD.
- Before deleting only this plan's WS, preserve its nonsecret reports/evidence/rulings and final review in durable reviewed documentation. Do not commit private exports, tfvars, full state/plan, credentials or download URLs. Do not remove the worktree or sibling workspaces.
- No merge, shared push, publish, security-setting mutation or production invocation has been approved. Complete reviewable local work before any final action request; use the finishing skill and explain its exact permission requirement when asking.

## Current controller handoff (E6 in progress, 2026-09-08)

E5 is independently approved through 7eb35b4. Final real state-backed plan completed once at c9a2a6f with inputs unchanged through 7eb35b4 and scope accepted after review: two cache-policy creates, four in-place updates (distribution, edge function, alarm description, producer concurrency), and self-logging association removal only. See terraform-final-read-only-plan-report.md and sanitized evidence/terraform-final-read-only-plan-summary.json. Full private state/plan/inputs are deliberately absent. No apply or public release occurred.

E6 corrects measured combined phone+200%-text overflow under Rulings23/24 and records final validation on its changed web artifact. E5's full 94-browser/63-backend/17-Node result is exact for 3408657; subsequent pipeline and plan-classifier checks are scoped and their unchanged package identities documented. E6 must provide final web identity and affected suite results, while unchanged backend/package/infrastructure evidence stays valid with identity checks. E6 QA preparation e6-browser-pdf-acceptance.md includes actual contrast/PDF/settled-graph measurements and explicitly distinguishes early faulty QA harness/timing captures.

Deferred observations for independent triage: inherited Autoprefixer resolution warning (E5), current PDF generator checks annotation count rather than exact URI identities (actual PDF exact URIs separately verified), temporary fixed-menu overlap during arbitrary scrolling (E6; anchor/focus destinations remain clear). See original reports and progress Ruling25; controller dispositions are reviewable reasoning, not instructions to suppress findings.

Cleanup preflight: six older force-tracked files exist under this WS even though the directory is globally ignored: task-A2-report.md, task-C1-report.md and four c1 evidence files. Preserve all of them in the durable archive before deleting this plan's WS. The final cleanup commit must explicitly remove obsolete tracked scratch copies and verify byte-identical durable counterparts. Never delete sibling WS or the worktree.
