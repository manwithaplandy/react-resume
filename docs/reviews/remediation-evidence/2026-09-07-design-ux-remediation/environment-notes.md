Read-only access checks during setup:

- AWS CLI available; STS succeeds outside network sandbox with existing credentials.
- GitHub CLI authentication succeeds outside network sandbox; default sandbox failure was network-related, not invalid stored credentials.
- Terraform 1.14.7 available.
- Cloudflare connector is not installed; no account mode has been inspected.
- Production setting changes, pushes and releases have not occurred.

CloudFront read-only snapshot (2026-09-08 UTC):
- Distribution EDHU4C51HW4BG, d2v6o77xftr5if.cloudfront.net; apex and www aliases.
- Default behavior routes to S3-mostly-upward-lion-website-bucket, origin mostly-upward-lion-website-bucket.s3.us-west-1.amazonaws.com with OAC E3Q6QZXOS9AMQ8. Two additional custom origins for apex/www are present but not the default target.
- Viewer protocol allow-all; managed CachingDisabled ID 4135ea2d-6df8-44a3-9df3-4b5a84be39ad; compression false; no custom error responses.
- Custom ACM certificate exists in us-east-1, sni-only, TLSv1.2_2021. Certificate validity and Cloudflare mode still require inspection.
- CloudFront logging targets mostly-upward-lion-log-bucket.s3.amazonaws.com prefix cloudfront-logs/, no cookies.

Direct-origin probe 2026-09-08 04:13 UTC: curl --head --connect-to andrewmalvani.com:443:d2v6o77xftr5if.cloudfront.net:443 https://andrewmalvani.com/ returned HTTP/2 200 with normal certificate validation, HTML, S3 origin and Miss from CloudFront. No Cache-Control header was returned. This proves this tested HTTPS origin path works; it does not establish Cloudflare mode.

ACM read-only verification: certificate ISSUED, covers andrewmalvani.com and *.andrewmalvani.com, expires 2027-03-10T16:59:59-07:00, attached to the identified distribution.

S3 get-bucket-logging verified 2026-09-08: mostly-upward-lion-log-bucket LoggingEnabled targets itself, prefix this-bucket-log/. This confirms F18 in live configuration; nothing was changed.

Public analytics baseline saved locally to baseline-stats.json and baseline-stats.headers (2026-09-08 UTC). SHA-256 4cf83c94e6abd745b29707a665d381b6ee8d3fbd74e79daaf55cec69a6b2a24c. This is the public JSON only, not a table backup/export; D4 still requires a secured aggregate/ledger backup before rollout.

Cloudflare dashboard access check: temporary Codex in-app browser tab navigated to dash.cloudflare.com and redirected to /login with Sign in to Cloudflare. No authenticated account settings available in this browser; no login attempted, no token retrieved, and temporary tab closed. Mode/proxy/cache-rule verification remains dependent on authenticated account access.

## Local Terraform validation preflight

On the unchanged Terraform source at A2 head 4efb1d0, controller ran `terraform -chdir=terraform init -backend=false -input=false -no-color` with network escalation. Installed locked archive 2.4.2, AWS 5.50.0 and random 3.6.2. Init removed the unused null provider from the lockfile; controller restored the exact HEAD lockfile to avoid an unrelated source change and copied the already cached locked null 3.2.2 darwin_arm64 provider binary from the original checkout into this worktree's ignored provider cache. No application/configuration source changes remain from setup.

`terraform -chdir=terraform validate -no-color` with escalation passed: `Success! The configuration is valid.` Sandbox-only validation failed provider IPC handshake, so local validation needs the same approved escalation. Backend remains disabled; no plan/apply or live resource changes occurred. For later init use `-lockfile=readonly` where appropriate to retain existing lock selections. Providers are cached; do not repeat setup unless an actual dependency change requires it. Revalidate Terraform after source changes in relevant D/E tasks.

Bundled Python 3.12 preflight: reportlab and pypdf are installed; boto3 and botocore are not. Analytics test tasks should explicitly provide any needed test dependencies in an isolated environment and document the reproducible CI setup; do not confuse a missing local SDK with invalid AWS credentials.

## Ready analytics SDK test environment

Controller created an isolated Python 3.12 environment at `/private/tmp/react-resume-stats-venv`. Use `/private/tmp/react-resume-stats-venv/bin/python` for analytics tests that need SDK validation. Installed boto3 1.43.89 / botocore 1.43.89, matching the AWS-maintained [PyPI release verified on execution date](https://pypi.org/project/boto3/1.43.89/). Transitives: jmespath 1.1.0, python-dateutil 2.9.0.post0, s3transfer 0.19.2, six 1.17.0, urllib3 2.7.0. Imports and offline DynamoDB TransactWriteItems service-model loading pass; model maximum is 100 actions. pip check reports no broken requirements (its cache-path warning is environmental; use PIP_NO_CACHE_DIR=true for later pip commands). No AWS client/request or cloud write was executed in this preflight.

D2/D3 must declare their needed test SDK dependencies durably for E5 CI and use explicit fake service clients / synthetic credentials / disabled metadata lookup where relevant. Do not let tests use the owner's AWS login. SDK shape validation can use the local service model without network. The ephemeral environment is convenience, not the repository's install contract.

Public cache baseline observed 2026-09-08 07:12:53–07:12:56 UTC, two ordinary unauthenticated GETs per object with normal certificate verification, no cache-busting query. Evidence: evidence/delivery-cache-before.json. HTML: no Cache-Control/Age, CF DYNAMIC and CloudFront Miss both times. stats.json: max-age=3600, CF DYNAMIC/CloudFront Miss both times. PDF and actual HTML-referenced /_next/static/chunks/polyfills-c67a75d1b6f99dc8.js: max-age=14400, first CF MISS then HIT (Age 0), cached origin X-Cache remains Miss. This shows Cloudflare can cache static assets despite disabled CloudFront cache; the cached X-Cache header does not establish a second origin request. Do not infer authenticated cache rules or SSL mode from these observations. Full-body elapsed 0.18–0.704s is a small sample, not field performance evidence or a guaranteed speedup. No source/deployment/account changes. E4 use this existing baseline instead of repeated setup requests; E6 preserve meaningful values/method in durable operations record.

Log-prefix baseline observed 2026-09-08 07:17:28 UTC: read-only ListObjectsV2 aggregated Size metadata, never fetched log bodies or retained object keys. evidence/log-prefix-baseline.json. cloudfront-logs/: complete listing 11,894 current objects / 15,127,314 bytes (12 pages); website-log/: complete listing 71,198 / 58,489,829 bytes (72 pages); this-bucket-log/: bounded listing stopped at 100 pages, at least 100,000 current objects / 113,812,996 bytes, NOT a complete count. Versions and delete markers excluded; listings are not atomic snapshots. Do not present the bounded self-log result as a total, exact growth baseline or measured savings. Keep the cap/method explicit; later growth comparison needs a comparable complete listing or another justified metric. No resource settings changed. Use this existing baseline rather than repeat large setup scans without a new reason.

Terraform input preflight: no *.tfvars file exists in this execution worktree. Original checkout has /Users/andrew/Scripts/react-resume/terraform/terraform.tfvars; controller inspected variable names only and found email_address. No values were printed/copied. cloudflare_zone_id defaults to empty in source but is supplied by the main workflow repo variable, so a real plan must recover/preserve the actual configured value through authorized inputs, not accept empty merely because local defaults permit it. Backend-disabled validation has no input requirement and remains distinct from a real state-backed plan. Do not initialize/migrate the execution worktree backend or save state/plan secrets to source casually; prepare a separate explicitly scoped plan environment and report unavailable inputs/access precisely. No real Terraform plan/backend connection has yet run.

Terraform input follow-up: controller successfully read the existing GitHub repository CLOUDFLARE_ZONE_ID variable without printing its value and combined it with the original checkout's unchanged tfvars in /private/tmp/react-resume-infra-review-vu595k01/inputs.auto.tfvars (parent0700/file0600). Inputs contain email_address and cloudflare_zone_id. Use this private existing-input file for a later scoped real infrastructure review; never print/copy its values into source, reports or tool output. This resolves the input-availability issue above; still no backend initialization or real plan/apply has run. Controller will coordinate one final state-backed plan after all relevant configuration changes rather than redundant intermediate remote plans. Task agents should run backend-disabled validation and describe expected scope; do not claim a reviewed real plan yet. The final review environment should remain separate from the execution checkout, with private plan/state outputs.

Cloudflare API access follow-up: the repository deliberately documents its existing SSM integration token as Analytics:Read (terraform/statsLambda.tf local token comment). Cloudflare's current primary API reference for GET /zones/{zone_id}/settings/{setting_id} requires Zone Settings Read or Write: https://developers.cloudflare.com/api/resources/zones/subresources/settings/methods/get/ . The analytics integration therefore is not established settings access; no secret value was retrieved, printed, or tested, and no settings permission was inferred. F19 still needs authenticated settings evidence or owner-supplied nonsecret mode/proxy/cache details. Do not broaden or rotate the analytics token to solve this discovery gap.

E4 cross-CDN verification detail (primary Cloudflare docs verified 2026-09-08): Browser Cache TTL can increase a lower origin max-age, and applicable cache rules can override origin directives. Therefore prepared S3/CloudFront60s browser/300s shared metadata alone cannot establish end-to-end freshness while Cloudflare settings remain unknown. Inspect Browser Cache TTL (including Respect Existing Headers behavior) and matching browser/edge Cache Rules before release; record coordinated setting changes for approval, then verify actual public headers. Our observed PDF/chunk max-age14400 is public behavior, not proof of which account setting produced it. Cache purge does not clear visitors' existing browser caches. Primary sources: https://developers.cloudflare.com/cache/how-to/edge-browser-cache-ttl/set-browser-ttl/ and https://developers.cloudflare.com/cache/concepts/cache-control/ . Do not promise the declared freshness window from Terraform compilation alone.

## Secured analytics baseline preflight
2026-09-08T09:07:13–17UTC: read-only DynamoDB scan with ConsistentRead=true and known-family filter completed5pages,59,207 items. Private0700 directory /private/tmp/react-resume-analytics-baseline-i9e9w4oa holds0600 aggregate-and-processing-items.json, stats-before.json, summary.json. No raw logs or unrelated families fetched; no producer paused/invoked and no cloud mutation. Summary only copied to evidence/analytics-private-baseline-summary.json. ExportSHA2564768498f9b999faedd04a60f7a680cd7c63b1ba4494a267c1619849deea20ee5; payloadSHA2564cf83c94e6abd745b29707a665d381b6ee8d3fbd74e79daaf55cec69a6b2a24c. Total/day/page sums all57,535 and match public total. Known families:58,187legacy markers,869document days(2024-04-02..2026-09-07),102CFdaily,45referrer,2page,1total,1cursor. No source/logv2items. Strong per-item reads are not atomic snapshot; this is a preflight baseline and a writer-quiesced cutover backup remains required before migration.

Additional local-only analysis of the same private export (no repeated AWS calls): stored CFdaily102days2026-05-29..2026-09-07, dailyunique sum37,253(matchespublic), countryrequestsum450,316; document869stored days across889calendar days2024-04-02..2026-09-07,20absentitemdates. These are stored-row coverage bounds, not guaranteed collection completeness; absent rows cannot establishzero. SummaryJSON updated with these aggregate-only relationships.

Controller read-only live retention verification2026-09-08: S3GetBucketLifecycleConfiguration confirms Enabled rules forcloudfront-logs/,website-log/,this-bucket-log/: currentexpiration90days; noncurrentversionexpiration30days. Source main.tf matches. Evidence/live-log-retention.json holds nonsecret response summary/boundary. This verifies configuredpolicy, not exactphysicaldeletiontime or independentlyauditedrawdata. Noncurrentage starts whenversionbecomesnoncurrent, not originalcreation; do notcollapsebothinto90-daytotalretentionclaim. Noobjectbodiesread, settingsunchanged.

Controller D4 input-availability preflight completed 2026-09-08T09:35:25–32UTC via read-only ListObjectVersions restricted to cloudfront-logs/. Complete 25-page listing: 15,118 listed object versions, 9,643 delete markers, 11,902 current keys (key dates June9–Sept8), and 3,216 keys available only as noncurrent versions (all version key dates May9–Sept8). Against the earlier private export’s 58,187 legacy markers: 11,853 current matches, 3,216 noncurrent-only matches, 43,118 keys with no listed version. There are 49 current keys without an exported marker; the export and listing are at different times and the producer was not paused, so this does not establish a processing defect. Private metadata remains /private/tmp/react-resume-analytics-baseline-i9e9w4oa/cloudfront-log-version-metadata.json (0600); summary only at evidence/log-availability-summary.json. No object bodies were read, restored or replayed. Listed versions establish metadata availability, not content integrity/replay safety; external backups remain uninspected. Do not claim globally permanent loss or exact reconstructability. Consume this completed baseline instead of repeating a large listing. Primary API semantics: https://docs.aws.amazon.com/AmazonS3/latest/API/API_ListObjectVersions.html .

Additional transport baseline 2026-09-08T09:46:52–54UTC, evidence/delivery-entry-paths-before.json: public HTTP apex and www each return one Cloudflare301 to same-host HTTPS then200. HTTPS www homepage/graph and apex graph/stats return200 without redirects. Direct www HTTPS origin via curl --connect-to retaining www hostname/Host/SNI also returns200 with normal certificate validation (ssl_verify_result0); this complements the earlier apex origin check. No certificate errors ignored. These prove observed entry-path behavior and both origin hostname prerequisites, not authenticated Cloudflare SSL mode/proxy/cache settings. No production configuration changed. Existing delivery-cache-before.json already records successful PDF/statsJSON/actual-chunk requests; do not repeat preflight solely for freshness. Post-release verification remains distinct.


GitHub enforcement preflight (2026-09-08T10:25:41Z): read-only main branch protection returns required_status_checks:null, enforce_admins:false, required_approving_review_count:0; repository rulesets listing returned no entries. Adding PR jobs is not equivalent to enforcing them as merge requirements. Evidence: evidence/github-check-enforcement-before.json. Prepare exact new required check names after E5 job design; repository-settings enforcement remains a distinct authorized release setting change, not performed during implementation. No GitHub settings changed.


Completed read-only deployed-package baseline, 2026-09-08T10:27:43Z: private0600 /private/tmp/react-resume-analytics-baseline-i9e9w4oa/statsAggregator-before.zip downloaded through AWS-generated temporary URL without printing/saving that URL or Lambda environment. SHA256e3f30b13d0335aa5d898d2c21f71ef9153d769f96e07510e9be137f2254eee51 matches Lambda CodeSha256; zip6761bytes contains exactly lambda_function.py. Runtimepython3.12, handlerlambda_function.lambda_handler, active/successful, no reserved concurrency. Existing daily rule enabledcron(0 0 * * ? *) with one target matching this Lambda. Nonsecret evidence evidence/deployed-analytics-package-before.json. No invocation/schedule/concurrency/code mutation. Consume this evidence for D4Step4; old package is for investigation, unsafe as post-ledger rollback. Temporary preflight package is not a durable quiesced release backup; retain/revalidate privately at approved release.


Workflow validator now available: /private/tmp/react-resume-actionlint-1.7.12/actionlint (official releasev1.7.12,darwin_arm64), prepared by controller from the official release asset with published SHA256 match aba9ced2dee8d27fecca3dc7feb1a7f9a52caefa1eb46f3271ea66b6e0e6953f. Version command passes. Use after actual workflow edits for semantic/static validation; this does not substitute for the approved GitHub PR/failing/passing runs. Documentation https://github.com/rhysd/actionlint/blob/main/docs/install.md and exact release https://github.com/rhysd/actionlint/releases/tag/v1.7.12. No global install/source dependency change.

Actionlint1.7.12 official gh attestation verify succeeded (exit0), in addition to published-checksum verification.

Controller actionlint baseline: immutable 4180436:.github/workflows/main.yml copied to tasktemporarydirectory and checked with official actionlint1.7.12; exit0,nooutput. This is only static syntax/semantics evidence for the old workflow; final edited workflow still needs owning-task validation and approved GitHub run evidence.

CloudFront→S3 transport follow-up: live GetOriginAccessControl E3Q6QZXOS9AMQ8 returned OriginTypes3,SigningBehavioralways,SigningProtocolsigv4. This is the previously verified default S3 origin attachment onEDHU4C51HW4BG. AWS primary docs statealways-sign S3 OAC usesHTTPS: https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/private-content-restricting-access-to-s3.html (Prerequisites). Evidence delivery-origin-access-control.json. This proves the configuredCloudFront→S3 transport contract, not a packetcapture or the Cloudflare→CloudFront hop/accountSSLmode. No settings changed.

D4 asynchronous cutover follow-up11:51–52UTC: no explicit Lambda EventInvokeConfig forstatsAggregator:$LATEST; EventBridge schedule target RetryPolicy/DeadLetterConfig null. Existing read-only identity/runtime/schedule details remain unchanged. Official absent-override defaults Lambda21600s/error retries2 andEventBridge86400s/up to185delivery attempts. Zero reserved concurrency sends NEW async events to configuredDLQ/failure destination without retries; do not claim every zero-concurrency arrival queues6h. DisableRule propagation is not instant and has no exact bound in the API doc. Oldqueuedwork/upstreamdelayedretry/otherinvokers require provenquiescence beforecutoverbackup, especially because Terraform reappliesD3concurrency1beforethecodeupdate job. Noqueuecontentsobserved/settingschanged/pauseperformed. Evidence analytics-async-retry-before.json includes exactprimaryreferences; consume it rather than repeat discovery.

Precise E1 init report correction: strict-backend=false-lockfile=readonly FAILED because the obsolete unusednull entry requires pruning; readonlydidnotpruneit. Non-readonlybackenddisabledinitreusedarchive2.4.2/aws5.50.0/random3.6.2andprunedonlynull; validationpassedthenexacttrackedlockrestored. Ruling18 authorizes E5 to intentionallyremoveonlyconfirmedunusednullwithallusedproviderpins/hashesunchanged, so strictCIinitcanpasswithoutrelaxinglocks. Verifycurrentconfigurationhasnonulldependencyfirst. Finalrealplanwilluse reviewedfinaltrackedlock afterthisboundedmaintenance, not anunreportedmodifiedlock.
