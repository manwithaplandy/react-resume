**WBR-01: Back can show the homepage under a graph URL — ADDRESSED.** [GraphExplorer.tsx:147](/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/src/components/Graph/GraphExplorer.tsx:147) waits for router readiness; lines 176/178 and 267 use Next’s public shallow replacement/push methods with scrolling disabled. Initial selection still replaces, while selection and mode changes push. This preserves the router metadata needed to restore the graph after leaving it, and avoids competing with static-export query hydration.

**Coverage check:** [graph-access.spec.ts:305](/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/tests/e2e/graph-access.spec.ts:305) exercises initial replacement, changed selection and changed mode across graph → Classic resume → Back/Forward. It asserts the rendered graph/homepage, URL, pressed mode, expanded selection, deeper history restoration, absence of page errors and a retained document marker. The preserved RED fails all three cases on graph visibility after the graph URL returns. The final focused run passes 3/3; the six-step progressing-clock replay and inspected text/3D captures show the correct restored Python content.

**Contact verification check:** [contact-submission.spec.ts:240](/Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation/tests/e2e/contact-submission.spec.ts:240) now waits for actual Contact removal before boundedly polling the submitted signal. The focused context check at lines 201–208 confirms instrumentation subclasses the native AbortController; it does not manufacture an aborted result. The same-document assertion, delayed response release/settlement and absence of stale feedback after returning remain at lines 241–258. Contact product code is unchanged. Ruling28’s five-second poll remains shorter than the existing fifteen-second request timeout.

**Evidence check:** The final complete output records **100/100 passing cases, no skips**, including all three history cases and Contact cleanup; the focused combined run passes 4/4. Both typechecks and nonmutating lint pass. Earlier 2/3 and 99/100 attempts remain accurately identified as failures. The optional Contact whole-file formatting check still warns and is not claimed to pass.

**Identity check:** Current bytes of all three changed files match the final validation record for `f8ab5340c66e0d56f94d3837bdb681fb2e873e31`. The recorded final website manifest matches SHA256 `9adc540eeb85bdd1fd822f82efd8e2668cc931f587b4861d5623fdb926ad36c7`; the preserved record reports 144 source/configuration/content files unchanged after the final suite. No test suite, build, cloud query or production action was rerun for this review.

**New breakage in the fix diff: None identified.**

**Out-of-scope observations:** WBR-02’s fixed-menu overlap and WBR-03’s inherited Autoprefixer warning remain accepted deferred Minors. Exact-PDF-URI automation remains optional; the current PDF’s exact links remain verified. No additional out-of-scope issue was identified.

**Fix round: All findings addressed, no new Critical/Important breakage.** The sole whole-branch code merge blocker is resolved. Local mergeability is approved from this review, with the controller’s final evidence/ledger preservation and scoped scratch cleanup still to complete. Production readiness and authorization remain separate: owner, OS/accessibility, first-time-reader, GitHub, account, fresh-plan, quiescence/backup and live-release verification gates remain open.
