import {chromium,expect} from '@playwright/test';import {writeFileSync} from 'node:fs';const ws='.superpowers/sdd/2026-09-07-design-ux-remediation';const browser=await chromium.launch();const page=await browser.newPage({reducedMotion:'reduce',viewport:{width:320,height:844}});await page.route('**/*',r=>new URL(r.request().url()).origin==='http://127.0.0.1:3104'?r.continue():r.abort());
for(const width of [320,390,430]){await page.setViewportSize({width,height:844});await page.goto('http://127.0.0.1:3104/');await page.evaluate(()=>document.documentElement.style.fontSize='200%');await page.evaluate(()=>document.fonts.ready);await page.locator('#contact a[href^="mailto:"]').scrollIntoViewIfNeeded();await page.screenshot({path:`${ws}/evidence/e6-final-text200-email-${width}.png`});}
await page.setViewportSize({width:430,height:844});await page.goto('http://127.0.0.1:3104/graph?view=list#node=skill%3Apython');await expect(page.locator('[id="graph-details-skill:python"]')).toBeVisible();await page.getByRole('button',{name:'3D view',exact:true}).click();await expect(page.locator('canvas')).toBeVisible();await page.locator('canvas').scrollIntoViewIfNeeded();await page.waitForTimeout(10500);await page.screenshot({path:`${ws}/evidence/e6-final-graph-real-clock-python.png`});await page.getByRole('button',{name:'Show overview',exact:true}).click();await page.waitForTimeout(500);await page.locator('canvas').screenshot({path:`${ws}/evidence/e6-final-graph-real-clock-overview.png`});writeFileSync(`${ws}/evidence/e6-final-detail.json`,JSON.stringify({clock:'Real progressing Date/performance; no page.clock override',finalUrl:page.url(),width:430},null,2));await browser.close();console.log('DONE final link and graph detail');
