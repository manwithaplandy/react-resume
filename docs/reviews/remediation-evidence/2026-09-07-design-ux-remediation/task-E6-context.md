# Delivery and Quality Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give public URLs reliable recovery, verify secure and efficient delivery, and prevent the reviewed behaviors from regressing unnoticed.

**Architecture:** Keep the existing AWS/Cloudflare hosting arrangement and Terraform resource identities. Separate route recovery, logging, transport, caching and CI into individually reviewable changes. Use read-only live discovery to resolve external configuration before preparing any coordinated production change.

**Tech Stack:** Next.js static export, Terraform with the current provider lock, CloudFront Functions, S3, Cloudflare, GitHub Actions, Node test runner, Playwright and Python unittest.

**Spec:** [Quality report](/Users/andrew/Scripts/react-resume/reports/design-ux-review-2026-09-07.md), F15, F18, F19, F28–F30 and verification of F14. [Master plan](/Users/andrew/Scripts/react-resume/docs/superpowers/plans/2026-09-07-design-ux-remediation.md).

## Global Constraints

- Preserve resource identities, data, existing site hostnames, the separately published `stats.json`, and the known working origin path.
- Do not change only the CloudFront HTTPS redirect setting while Cloudflare is still Flexible; the repository documents a redirect loop.
- Keep current log retention unless an independently reviewed policy changes it. Disabling self-logging does not authorize deleting stored logs or buckets.
- Resolve actual account/resource identifiers through read-only discovery; do not invent IDs or expose secrets in plans, test output or commits.
- Build and inspect a concrete Terraform plan before applying it under the execution authorization in force. An environment/approval-system block must be reported, not bypassed; unrelated work can continue.
- No fix-code snippets are supplied. Commands and behavioral tests support task execution.

---

## File map

| Responsibility | Files |
|---|---|
| Edge routing and useful 404 | Modify [main Terraform](/Users/andrew/Scripts/react-resume/terraform/main.tf:338); create `/Users/andrew/Scripts/react-resume/terraform/functions/rewrite-extensionless.js`, `/Users/andrew/Scripts/react-resume/src/pages/404.tsx`, `/Users/andrew/Scripts/react-resume/tests/infra/edge-routing.test.mjs`, `/Users/andrew/Scripts/react-resume/tests/e2e/not-found.spec.ts` |
| Logging | Modify [self-logging resource](/Users/andrew/Scripts/react-resume/terraform/main.tf:117); retain lifecycle resources |
| Transport and caching | Modify [delivery behavior](/Users/andrew/Scripts/react-resume/terraform/main.tf:281), [public outputs](/Users/andrew/Scripts/react-resume/terraform/outputs.tf:1), [workflow](/Users/andrew/Scripts/react-resume/.github/workflows/main.yml:1); coordinate stats cache metadata with the [producer](/Users/andrew/Scripts/react-resume/stats_aggregator/lambda_function.py:449) |
| Quality workflow | Create `/Users/andrew/Scripts/react-resume/.github/workflows/checks.yml`; modify existing main workflow and A1-owned scripts only after their changes merge |
| Documentation and evidence | Modify [README](/Users/andrew/Scripts/react-resume/README.md:1), [historical review](/Users/andrew/Scripts/react-resume/DESIGN_REVIEW.md:1); create `/Users/andrew/Scripts/react-resume/docs/operations/delivery.md` and `/Users/andrew/Scripts/react-resume/docs/reviews/design-ux-remediation-status.md` |


## Execution context

Work only in /Users/andrew/Scripts/react-resume/.worktrees/design-ux-remediation. Paths in the source plan point to the original checkout; map them into this worktree. The user has now authorized implementation. Do not deploy/push or change cloud settings in a task; prepare reviewable changes and record exact external dependencies. Do not invent missing factual evidence.

## Verified execution tools

Use Node 22 at /Users/andrew/.nvm/versions/node/v22.16.0/bin/node (prepend that bin directory to PATH for Yarn commands). Default PATH selected Node 26 during setup. Python 3.12.14 is available at /Users/andrew/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3. Network-bound calls may require require_escalated; AWS and GitHub credentials were verified by read-only calls outside the sandbox. See environment-notes.md for already performed live checks; do not repeat them without a new reason.

## Owner and release dependencies

The controller requested residence, metric reconciliation, credential evidence and two project narratives asynchronously. No answer has arrived as of setup. Check with the controller at task start for new input. Complete all code/design/verification work independent of these answers; preserve original factual contexts and record exact unresolved rows. External releases, shared pushes and security-setting changes require the controller’s final reviewable handoff; prepare code and read-only evidence without performing those actions. Local completion and production verification are different statuses.

Preserve any evidence required by final closure in durable repository documentation before the SDD scratch workspace is cleaned. The final status table must not depend solely on screenshots or reports that will be deleted with .superpowers/sdd/. Summarize actual commands/results/commit IDs and retain only useful final screenshots as review artifacts, not source assets.

For clean subsequent validation output, use `env -u NO_COLOR` with the Node 22 PATH; B1 reported conflicting color environment warnings. If Yarn reports unwritable cache/global directories in the sandbox, select task-scoped writable temporary paths or use the already authorized escalation, rather than changing application behavior or suppressing test failures.

Verification efficiency: after focused and relevant combined checks pass on the final source, do not repeat the same unchanged suite merely to label it fresh. Rebuild/rerun only when an actual change, failure, or unresolved concern warrants it; record exactly what source/results were verified.

During C1 controller inspected evidence/c1-contact-summary-320.png: form/summary fit and are readable; floating mobile menu overlays part of contact introductory text at that captured scroll position. Check the actual reading journey in final mobile review, determine whether it is the expected temporary fixed-button overlap or a material unresolved UX issue, and report evidence without silently broadening the remediation scope.

Runner follow-up: Yarn 1 still warned with YARN_GLOBAL_FOLDER alone in C1. A2 produced clean output using PREFIX=/private/tmp/react-resume-prefix and YARN_CACHE_FOLDER=/private/tmp/react-resume-yarn-cache plus env -u NO_COLOR. Prefer that known-working environment for subsequent checks; do not rerun completed tasks solely for this cosmetic runner warning.

Contact placeholders were identified for C2 actual contrast measurement and minimal correction under F17; verify its report covers the shared field style in final contrast closure.

Capture discipline from A3: Playwright visible assertions alone do not prove a Reveal section has finished fading. Scroll relevant sections into view and verify real opacity/transform have settled before screenshots/contrast sampling; preserve complete labels/rows rather than transition-frame crops. Do not claim screenshot readability from partial-opacity captures.

A3 reviewer deferred minors for final documentation/orientation: (1) resume-source.md uses a workstation-specific bundled Python path and lacks a durable dependency declaration. Provide portable Python setup/invocation and a small pinned PDF dependency declaration (verified reportlab 4.4.9 and pypdf 6.10.0; generator imports these). This belongs with the already approved generator's documented local dependencies. (2) generator postcondition counts link annotations rather than inspecting exact mailto/LinkedIn URIs. Current PDF was explicitly URI-verified. Document the precise guard and required manual/link-identity check; record this optional future guard hardening without claiming it is already implemented. Do not silently expand a documentation task into a generator logic fix loop.

Analytics test runtime update: a ready isolated Python 3.12 environment is available at /private/tmp/react-resume-stats-venv/bin/python with boto3/botocore 1.43.89 and validated offline DynamoDB models. See environment-notes.md for pinned versions. Add a durable test dependency declaration in the owning analytics task; use fake services/synthetic credentials and avoid the owner’s live AWS session in tests.

Public cache baseline observed 2026-09-08 07:12:53–07:12:56 UTC, two ordinary unauthenticated GETs per object with normal certificate verification, no cache-busting query. Evidence: evidence/delivery-cache-before.json. HTML: no Cache-Control/Age, CF DYNAMIC and CloudFront Miss both times. stats.json: max-age=3600, CF DYNAMIC/CloudFront Miss both times. PDF and actual HTML-referenced /_next/static/chunks/polyfills-c67a75d1b6f99dc8.js: max-age=14400, first CF MISS then HIT (Age 0), cached origin X-Cache remains Miss. This shows Cloudflare can cache static assets despite disabled CloudFront cache; the cached X-Cache header does not establish a second origin request. Do not infer authenticated cache rules or SSL mode from these observations. Full-body elapsed 0.18–0.704s is a small sample, not field performance evidence or a guaranteed speedup. No source/deployment/account changes. E4 use this existing baseline instead of repeated setup requests; E6 preserve meaningful values/method in durable operations record.

Log-prefix baseline observed 2026-09-08 07:17:28 UTC: read-only ListObjectsV2 aggregated Size metadata, never fetched log bodies or retained object keys. evidence/log-prefix-baseline.json. cloudfront-logs/: complete listing 11,894 current objects / 15,127,314 bytes (12 pages); website-log/: complete listing 71,198 / 58,489,829 bytes (72 pages); this-bucket-log/: bounded listing stopped at 100 pages, at least 100,000 current objects / 113,812,996 bytes, NOT a complete count. Versions and delete markers excluded; listings are not atomic snapshots. Do not present the bounded self-log result as a total, exact growth baseline or measured savings. Keep the cap/method explicit; later growth comparison needs a comparable complete listing or another justified metric. No resource settings changed. Use this existing baseline rather than repeat large setup scans without a new reason.

A5 runtime limitation: actual macOS reduced-motion switching and actual OS background-tab switch were unavailable. Browser media emulation, synthetic visibility event/property, real offscreen scrolling, real bitmap/frame stability, loop instrumentation, active-menu interruption and detached-canvas cleanup all passed in 42-test combined run at 3827362. Keep unavailable manual checks separate from those verified behaviors; evidence/a5-*.log and task-A5-report.md.

Terraform input preflight: no *.tfvars file exists in this execution worktree. Original checkout has /Users/andrew/Scripts/react-resume/terraform/terraform.tfvars; controller inspected variable names only and found email_address. No values were printed/copied. cloudflare_zone_id defaults to empty in source but is supplied by the main workflow repo variable, so a real plan must recover/preserve the actual configured value through authorized inputs, not accept empty merely because local defaults permit it. Backend-disabled validation has no input requirement and remains distinct from a real state-backed plan. Do not initialize/migrate the execution worktree backend or save state/plan secrets to source casually; prepare a separate explicitly scoped plan environment and report unavailable inputs/access precisely. No real Terraform plan/backend connection has yet run.

Terraform input follow-up: controller successfully read the existing GitHub repository CLOUDFLARE_ZONE_ID variable without printing its value and combined it with the original checkout's unchanged tfvars in /private/tmp/react-resume-infra-review-vu595k01/inputs.auto.tfvars (parent0700/file0600). Inputs contain email_address and cloudflare_zone_id. Use this private existing-input file for a later scoped real infrastructure review; never print/copy its values into source, reports or tool output. This resolves the input-availability issue above; still no backend initialization or real plan/apply has run. Controller will coordinate one final state-backed plan after all relevant configuration changes rather than redundant intermediate remote plans. Task agents should run backend-disabled validation and describe expected scope; do not claim a reviewed real plan yet. The final review environment should remain separate from the execution checkout, with private plan/state outputs.

Cloudflare API access follow-up: the repository deliberately documents its existing SSM integration token as Analytics:Read (terraform/statsLambda.tf local token comment). Cloudflare's current primary API reference for GET /zones/{zone_id}/settings/{setting_id} requires Zone Settings Read or Write: https://developers.cloudflare.com/api/resources/zones/subresources/settings/methods/get/ . The analytics integration therefore is not established settings access; no secret value was retrieved, printed, or tested, and no settings permission was inferred. F19 still needs authenticated settings evidence or owner-supplied nonsecret mode/proxy/cache details. Do not broaden or rotate the analytics token to solve this discovery gap.

B2 verified real WebGL layout at320/390/844-landscape/1280, 200% root-text enlargement, actual container/canvas resizing, mode/history focus, contrast7.11–12.09 and touch vertical-pan behavior. Controller viewed all key before/after captures and onscreen320/390 canvas images. Physical hardware rotation, OS preferences UI and browser chrome zoom were unavailable; preserve that distinction. Commit88293f2, task-B2-report.md, evidence/b2-*.png and b2-rendered-contrast.json.

E4 cross-CDN verification detail (primary Cloudflare docs verified 2026-09-08): Browser Cache TTL can increase a lower origin max-age, and applicable cache rules can override origin directives. Therefore prepared S3/CloudFront60s browser/300s shared metadata alone cannot establish end-to-end freshness while Cloudflare settings remain unknown. Inspect Browser Cache TTL (including Respect Existing Headers behavior) and matching browser/edge Cache Rules before release; record coordinated setting changes for approval, then verify actual public headers. Our observed PDF/chunk max-age14400 is public behavior, not proof of which account setting produced it. Cache purge does not clear visitors' existing browser caches. Primary sources: https://developers.cloudflare.com/cache/how-to/edge-browser-cache-ttl/set-browser-ttl/ and https://developers.cloudflare.com/cache/concepts/cache-control/ . Do not promise the declared freshness window from Terraform compilation alone.

Controller read-only live retention verification2026-09-08: S3GetBucketLifecycleConfiguration confirms Enabled rules forcloudfront-logs/,website-log/,this-bucket-log/: currentexpiration90days; noncurrentversionexpiration30days. Source main.tf matches. Evidence/live-log-retention.json holds nonsecret response summary/boundary. This verifies configuredpolicy, not exactphysicaldeletiontime or independentlyauditedrawdata. Noncurrentage starts whenversionbecomesnoncurrent, not originalcreation; do notcollapsebothinto90-daytotalretentionclaim. Noobjectbodiesread, settingsunchanged.

Additional transport baseline 2026-09-08T09:46:52–54UTC, evidence/delivery-entry-paths-before.json: public HTTP apex and www each return one Cloudflare301 to same-host HTTPS then200. HTTPS www homepage/graph and apex graph/stats return200 without redirects. Direct www HTTPS origin via curl --connect-to retaining www hostname/Host/SNI also returns200 with normal certificate validation (ssl_verify_result0); this complements the earlier apex origin check. No certificate errors ignored. These prove observed entry-path behavior and both origin hostname prerequisites, not authenticated Cloudflare SSL mode/proxy/cache settings. No production configuration changed. Existing delivery-cache-before.json already records successful PDF/statsJSON/actual-chunk requests; do not repeat preflight solely for freshness. Post-release verification remains distinct.


GitHub enforcement preflight (2026-09-08T10:25:41Z): read-only main branch protection returns required_status_checks:null, enforce_admins:false, required_approving_review_count:0; repository rulesets listing returned no entries. Adding PR jobs is not equivalent to enforcing them as merge requirements. Evidence: evidence/github-check-enforcement-before.json. Prepare exact new required check names after E5 job design; repository-settings enforcement remains a distinct authorized release setting change, not performed during implementation. No GitHub settings changed.


Completed read-only deployed-package baseline, 2026-09-08T10:27:43Z: private0600 /private/tmp/react-resume-analytics-baseline-i9e9w4oa/statsAggregator-before.zip downloaded through AWS-generated temporary URL without printing/saving that URL or Lambda environment. SHA256e3f30b13d0335aa5d898d2c21f71ef9153d769f96e07510e9be137f2254eee51 matches Lambda CodeSha256; zip6761bytes contains exactly lambda_function.py. Runtimepython3.12, handlerlambda_function.lambda_handler, active/successful, no reserved concurrency. Existing daily rule enabledcron(0 0 * * ? *) with one target matching this Lambda. Nonsecret evidence evidence/deployed-analytics-package-before.json. No invocation/schedule/concurrency/code mutation. Consume this evidence for D4Step4; old package is for investigation, unsafe as post-ledger rollback. Temporary preflight package is not a durable quiesced release backup; retain/revalidate privately at approved release.


Controller mobile-menu preflight on copied D1fix4180436 export: actual native mobile-menu Contact and Portfolio anchor journeys at320x844, plus empty-form first-invalid-field focus, show no text/control intersecting the48x48 fixed menu rectangle. Controller inspected allthree viewportPNGs: headings start below control; error summary/firstfield readable. Evidence e6-mobile-overlap-preflight.json, e6-mobile-overlap-contact-anchor.png, e6-mobile-overlap-contact-invalid-focus.png, e6-mobile-overlap-portfolio-anchor.png; script retained. No external requests/contact submission; copied static build served on separatelocal3102 thenserverstopped. This resolves those specific destination/focus risks, not every possible arbitrary scroll position or final all-width pass. No application change warranted solely by prior element/full-page capture overlap. Assess remaining integrated reading flow in E6 without presuming a broad Header redesign is needed.

D2fix1 (12c0e09) adds an internal accepted Cloudflare checkpoint in source#cloudflare: checkpointVersion/publicProjection plus matching source metadata, privacy-filtered/bounded public aggregates only. Preserve this record with ledger/counters during migration/rollback; whole-record deletion is not a supported recovery path because absence is indistinguishable from legacy storage. Prior privatebaseline intentionallyhasnosourceitems(olddeployedhandlerconfirmed). Release backup afterquiescing must includeanyestablishedsourcecheckpoint. See D2report/ops for uncertainty behavior; do notoverwrite this record with metadata-only writes or classify it as disposable processing debris.

D3 Ruling17 adds one bounded no-TTL ingestion#active record (hashed expected completion identity/digest and numeric count metadata). Preserve it with logv2chunk/completion records, counters, source#cloudflare acceptedcheckpoint and existingcursor duringcutover/rollback. It detects unfinishedinput even if S3 no longer lists it; deleting it casually can permit publication of partialcounters. Private preflight export predates this newformat and rightlycontainsnone. Newrelease backup/export family selection must include thisguard; manual recovery remains evidence-backed, not marker-first replay or data reset.

CloudFront→S3 transport follow-up: live GetOriginAccessControl E3Q6QZXOS9AMQ8 returned OriginTypes3,SigningBehavioralways,SigningProtocolsigv4. This is the previously verified default S3 origin attachment onEDHU4C51HW4BG. AWS primary docs statealways-sign S3 OAC usesHTTPS: https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/private-content-restricting-access-to-s3.html (Prerequisites). Evidence delivery-origin-access-control.json. This proves the configuredCloudFront→S3 transport contract, not a packetcapture or the Cloudflare→CloudFront hop/accountSSLmode. No settings changed.

Controller E6 handoff preparation: e6-closure-preparation.md maps all30originalfindingcriteria/commit/evidence/dependencies; it is an input, not final statuses. E4completed06fda8b, E5running. E5isaskedtorunactualreusablewholechecks once onfinalrelevantsource andsaveoutputs/artifacts; ifE6changesonlydocumentation, consumethatpassandperformremainingvisual/manual/PDFacceptance withoutrebuilding/rerunningunchangedsuites solelyforfreshness. Anynewactualsourcechange/failurewarrantscoveringchecks.

Durableevidence target: docs/reviews/remediation-evidence/2026-09-07-design-ux-remediation/. BeforefinalWSdeletion, copy allnonsecretWSreports/evidence/rulings/briefs/reviewpackagesintothisreviewablearchive, preservingfilenamesandrelativeevidencepaths. CurrentWSis~9.8MiB/245files,mostlyPNG. Rootknown-secret-signaturescanfoundnomatches; thatheuristicdoesnotreplacemanualprovenanceinspection. No private/rawbackendstate/plans/tfvars/analyticsitemexports/zipdownloadURLs belonghere. RootwillappendfinalrealTFsummary/wholebranchreview/lastledgerupdates afteryourcommitandbeforefinalarchivecleanup. Linkstatusrowsinthedurabledirectory; preservehistoricaloutputverbatimratherthanrewritingstdout. Add anarchiveREADMEexplaininghistoricalabsoluteWSpathsandpublic-vs-privateevidenceboundaries.

Originalcheckoutpreservationpreflight: all23untrackedoriginaldocs/reportsfilesarebyteidenticaltocorrespondingbranchfiles; nonearedeleted/copied. Evidence/original-deliverable-preservation-preflight.json. Recheckifanylatermergeisapproved; currentworktreeonly.

CarryA3MinorportabilityevidenceintoyourREADME/PDFinstructions: exactgeneratorrequirements reportlab4.4.9 andpypdf6.10.0 wereused; missingpin/documentation shouldberesolvedinthisdocs-owningtask. Existinggeneratoronlycounts2URIannotations; actualtwoURItargetsweremanuallyverifiedA3. FinalPDFcheckmustrecordtargetidentitiesratherthanonlyannotationcount; generatorbehaviorchangeisnotrequiredsolelyforlowimpactdocs.

Evidence-archiveformatdetail: preservehistoricalreview-*.diff as deterministic .diff.gz files (gzipmtime0) withoriginalSHA256+storedSHA256inventory, ratherthanembedalmost0.8MiBofduplicatedsourcepatchtextagaininthefinalwholebranchdiff. Keep humanreports/evidencefiles normallyreadableandlinkable; archiveREADMEexplains gzipview/extractionandoriginalnames. Thisonlychangesdurablestoragenotanyhistoricaldiffcontent. Finalwholebranchreviewstillreceivesnewfullruntime/docsdiffplusarchivalmanifest/reports.

Ruling: Let E6 correct the direct email link's shrink/wrap behavior and add combined phone-width/200%-text coverage before closing F01 — final QA found a persistent390px overflow that the earlier desktop-only enlarged-text test did not cover, so documentation alone would leave a known High acceptance gap — cost if wrong: the email address can wrap across lines at large text sizes and E6 gains a small contact CSS/regression change plus a fresh affected build/browser pass; the address, contact behavior and owner facts remain unchanged.

FinalQAoffender: Contact/index.tsx44–55 directemailanchor/span,390viewport/200%roottext yieldslink462.42/right478.42/text28px. EarlierA1enlargedtexttestonly1280x900. Read e6-browser-pdf-acceptance.md whencomplete; nofix yet. BecauseE6nowhasactualUIchange, regeneratecheckedwebonceafterfixandrunnecessaryfinalstatic/browserchecks; reuseunchanged63backend/extractedarchive/TFresultswithidentityevidence,notmislabelold340exportaspostfix. Rootrealstateplancompletedc9; sanitizedreportandevidenceinWS,validifTF/packageinputsunchanged.

Ruling: Extend E6's enlarged-text correction to the complete observed320px set: About fact values, the résumé date, skill label/bar rows, and both Contact links — the direct-email overflow masked additional out-of-bounds or clipped content, so fixing only the largest offender would still leave F01 incomplete — cost if wrong: these rows may wrap or stack differently at large text sizes and require focused cross-section regression coverage; retain the chosen text size, complete content, normal-size layout and existing link/form behavior.

Read evidence/e6-overflow-200-matrix.json andfinalQAreportforfont-settledoffendergeometry. Add meaningful combined320/390/430+200%-textregressionthatdetectsinternalclipping aswellasdocumentscrollWidth; do notlower/clampusertextsizeorhidecontenttomakeitpass. Onebatchofnarrowwrapping/shrink/stackcorrectionsinobservedsections,notbroadredesign. E6sourcefixmaybeaseparatecommitbeforefinaldocscommit,allreviewedtogether.

Task E6: minor (deferred): final read-only QA measured temporary fixed-menu overlap at particular arbitrary scroll positions; anchor destinations and first-invalid-field focus remain clear, and further scrolling exposes the text. Evidence: e6-browser-pdf-acceptance.md and evidence/e6-arbitrary-*.png. Carry this observation into the durable status record and whole-branch review.

Ruling: Retain the existing fixed mobile-menu behavior for this implementation and record its transient overlap as a follow-up — the measured overlap is avoidable by scrolling and does not prevent access at the verified anchor/focus destinations, while changing global navigation behavior would exceed the narrow enlarged-text correction — cost if wrong: some readers may find the brief obstruction distracting and a later menu-position or reserved-space change may be needed; the final independent reviewer receives the evidence for triage.

Ruling: Extend E6's same enlarged-text correction to timeline body word wrapping and responsive credential badge/text stacking — final text-range measurements show the unbreakable Terraform/Terragrunt run still reaches392.92px at a320px viewport, and a fixed credential badge leaves only30px for credential text, causing clipping inside its card — cost if wrong: unusually long words may break across lines and credential cards may stack at large text sizes; preserve complete content, selected font size and normal horizontal card layout, with focused text-range and card-boundary regressions.

Ruling27: Preserve byte-exact historical archive whitespace and apply whitespace cleanliness to current authored source/docs, with the full archive-only warnings retained — rewriting CRLF headers/transcript spaces/brief endings would alter the evidence we promised to preserve — cost if wrong: whole-diff whitespace checks remain noisy and reviewers must distinguish archived records from maintained code; any warning outside the archive still requires correction.
